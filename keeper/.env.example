# Copy to .env and fill in — NEVER commit .env or keypairs
ANCHOR_PROVIDER_URL=https://api.devnet.solana.com
# ANCHOR_PROVIDER_URL=http://127.0.0.1:8899  # for localnet
PROGRAM_ID=
ANCHOR_WALLET=/home/user/.config/solana/id.json
# Hermes for live Pyth prices (required in production when feeds are real)
HERMES_URL=https://hermes.pyth.network
# NEVER set in production (keeper refuses NODE_ENV=production + ALLOW_MOCK_PRICES=1):
# ALLOW_MOCK_PRICES=1
