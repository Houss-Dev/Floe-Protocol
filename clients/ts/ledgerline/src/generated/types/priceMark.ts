/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getI32Decoder,
  getI32Encoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";
import {
  getFixedDecoder,
  getFixedEncoder,
  type Fixed,
  type FixedArgs,
} from ".";

/** A price mark for one asset at one instant, in USD fixed-point (1e9). */
export type PriceMark = {
  /** USD per whole share. */
  price: Fixed;
  /** Absolute confidence interval, USD per whole share. */
  conf: Fixed;
  /** Unix timestamp of the underlying feed update. */
  publishTs: bigint;
  /**
   * `(token_feed / equity_feed) - 1`, in basis points. Always surfaced to the
   * user, even when it is large — that honesty is the point.
   */
  dislocationBps: number;
};

export type PriceMarkArgs = {
  /** USD per whole share. */
  price: FixedArgs;
  /** Absolute confidence interval, USD per whole share. */
  conf: FixedArgs;
  /** Unix timestamp of the underlying feed update. */
  publishTs: number | bigint;
  /**
   * `(token_feed / equity_feed) - 1`, in basis points. Always surfaced to the
   * user, even when it is large — that honesty is the point.
   */
  dislocationBps: number;
};

export function getPriceMarkEncoder(): FixedSizeEncoder<PriceMarkArgs> {
  return getStructEncoder([
    ["price", getFixedEncoder()],
    ["conf", getFixedEncoder()],
    ["publishTs", getI64Encoder()],
    ["dislocationBps", getI32Encoder()],
  ]);
}

export function getPriceMarkDecoder(): FixedSizeDecoder<PriceMark> {
  return getStructDecoder([
    ["price", getFixedDecoder()],
    ["conf", getFixedDecoder()],
    ["publishTs", getI64Decoder()],
    ["dislocationBps", getI32Decoder()],
  ]);
}

export function getPriceMarkCodec(): FixedSizeCodec<PriceMarkArgs, PriceMark> {
  return combineCodec(getPriceMarkEncoder(), getPriceMarkDecoder());
}
