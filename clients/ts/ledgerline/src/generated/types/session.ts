/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getEnumDecoder,
  getEnumEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * Market session, derived from `Clock` + a holiday table.
 *
 * The session drives the haircut: the reference equity feed does not exist outside
 * US market hours, so the buffer widens when the underlying market is dark.
 */
export enum Session {
  Regular,
  Extended,
  Overnight,
  Closed,
}

export type SessionArgs = Session;

export function getSessionEncoder(): FixedSizeEncoder<SessionArgs> {
  return getEnumEncoder(Session);
}

export function getSessionDecoder(): FixedSizeDecoder<Session> {
  return getEnumDecoder(Session);
}

export function getSessionCodec(): FixedSizeCodec<SessionArgs, Session> {
  return combineCodec(getSessionEncoder(), getSessionDecoder());
}
