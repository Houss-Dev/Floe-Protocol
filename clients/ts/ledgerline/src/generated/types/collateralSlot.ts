/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";
import {
  getMarketKindDecoder,
  getMarketKindEncoder,
  type MarketKind,
  type MarketKindArgs,
} from ".";

/**
 * One collateral position. `raw_amount` is the on-chain token amount and is
 * never the UI amount — see the ScaledUiAmount finding in docs/RESEARCH.md.
 */
export type CollateralSlot = {
  mint: Address;
  /** PDA-owned token account holding this mint. */
  vaultAta: Address;
  /** Raw token amount, minor units (6dp for xStocks). */
  rawAmount: bigint;
  /**
   * The mint's ScaledUiAmount multiplier at deposit time, as f64 bits.
   * Used to compute the dividend delta later.
   */
  multiplierBits: bigint;
  /**
   * Copied from the asset when the slot was created: decides which
   * valuation/sizing rules this position lives under (`MarketKind`).
   * Stored on the slot, not read from the asset account at draw time,
   * because `draw` deliberately does not load per-asset accounts — the
   * line must stay a single-account hot path.
   */
  marketKind: MarketKind;
  inUse: boolean;
};

export type CollateralSlotArgs = {
  mint: Address;
  /** PDA-owned token account holding this mint. */
  vaultAta: Address;
  /** Raw token amount, minor units (6dp for xStocks). */
  rawAmount: number | bigint;
  /**
   * The mint's ScaledUiAmount multiplier at deposit time, as f64 bits.
   * Used to compute the dividend delta later.
   */
  multiplierBits: number | bigint;
  /**
   * Copied from the asset when the slot was created: decides which
   * valuation/sizing rules this position lives under (`MarketKind`).
   * Stored on the slot, not read from the asset account at draw time,
   * because `draw` deliberately does not load per-asset accounts — the
   * line must stay a single-account hot path.
   */
  marketKind: MarketKindArgs;
  inUse: boolean;
};

export function getCollateralSlotEncoder(): FixedSizeEncoder<CollateralSlotArgs> {
  return getStructEncoder([
    ["mint", getAddressEncoder()],
    ["vaultAta", getAddressEncoder()],
    ["rawAmount", getU64Encoder()],
    ["multiplierBits", getU64Encoder()],
    ["marketKind", getMarketKindEncoder()],
    ["inUse", getBooleanEncoder()],
  ]);
}

export function getCollateralSlotDecoder(): FixedSizeDecoder<CollateralSlot> {
  return getStructDecoder([
    ["mint", getAddressDecoder()],
    ["vaultAta", getAddressDecoder()],
    ["rawAmount", getU64Decoder()],
    ["multiplierBits", getU64Decoder()],
    ["marketKind", getMarketKindDecoder()],
    ["inUse", getBooleanDecoder()],
  ]);
}

export function getCollateralSlotCodec(): FixedSizeCodec<
  CollateralSlotArgs,
  CollateralSlot
> {
  return combineCodec(getCollateralSlotEncoder(), getCollateralSlotDecoder());
}
