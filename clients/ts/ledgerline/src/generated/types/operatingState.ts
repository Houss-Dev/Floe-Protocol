/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getEnumDecoder,
  getEnumEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * Two-level pause, so a bad keeper or a bad feed does not force an
 * all-or-nothing switch. Both non-Normal states keep the *unwinding* path
 * open — repay, withdraw, resize, liquidate, settle — because an incident
 * must never trap collateral: the borrower can always get out, and the
 * protocol can always be made whole. `Halt` additionally freezes harvest
 * (it would be mutating positions against a feed nobody trusts).
 */
export enum OperatingState {
  Normal,
  PauseDeposits,
  Halt,
}

export type OperatingStateArgs = OperatingState;

export function getOperatingStateEncoder(): FixedSizeEncoder<OperatingStateArgs> {
  return getEnumEncoder(OperatingState);
}

export function getOperatingStateDecoder(): FixedSizeDecoder<OperatingState> {
  return getEnumDecoder(OperatingState);
}

export function getOperatingStateCodec(): FixedSizeCodec<
  OperatingStateArgs,
  OperatingState
> {
  return combineCodec(getOperatingStateEncoder(), getOperatingStateDecoder());
}
