/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getMarketKindDecoder,
  getMarketKindEncoder,
  type MarketKind,
  type MarketKindArgs,
} from "../types";

export const ASSET_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  234, 180, 241, 252, 139, 224, 160, 8,
]);

export function getAssetDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ASSET_DISCRIMINATOR);
}

export type Asset = {
  discriminator: ReadonlyUint8Array;
  /** The Token-2022 mint carrying the `ScaledUiAmount` extension. */
  stockMint: Address;
  /**
   * Pyth `Equity.US.<TICKER>/USD` — the real underlying. Only exists in
   * regular session, which is exactly why the haircut widens.
   */
  equityFeedId: ReadonlyUint8Array;
  /** Pyth `Crypto.<TICKER>X/USD` — the token itself. Trades 24/7. */
  tokenFeedId: ReadonlyUint8Array;
  maxLtvRegularBps: number;
  maxLtvExtendedBps: number;
  maxLtvClosedBps: number;
  /** Liquidation triggers at this LTV, using the conservative mark. */
  liqThresholdBps: number;
  /** Never liquidate above this, even outside market hours. Hard floor. */
  liqFloorBps: number;
  /**
   * Discount a liquidator gets on the seized collateral, in bps. This is the
   * incentive to liquidate; without it nobody would bother.
   */
  liquidationBonusBps: number;
  /** Above this confidence ratio the LTV starts decaying. */
  maxConfRatioBps: number;
  /** Confidence floor applied when the equity feed is stale (`Closed`). */
  confFloorBps: bigint;
  /**
   * Largest multiplier change accepted as a dividend, in bps. A dividend
   * moves the multiplier by a fraction of a percent; a 2:1 split moves it
   * by 10,000 bps. Anything above this is treated as a corporate action we
   * do not understand and is refused rather than paid out.
   */
  maxMultiplierDeltaBps: number;
  /** Minimum on-chain pool depth, in whole USD, before the asset is usable. */
  minPoolDepthUsd: bigint;
  decimals: number;
  enabled: boolean;
  bump: number;
  /**
   * Public equity vs. pre-IPO collateral. Selects the rule set; see
   * `MarketKind`. Immutable after listing (`set_asset_params` will not
   * touch it — flipping the valuation rules under live positions would
   * be repricing someone's collateral behind their back).
   */
  marketKind: MarketKind;
  /**
   * Pre-IPO only: largest absolute divergence in bps between the sized
   * Pyth mark and the issuer's own published mark that `resize_line` will
   * accept. `validate_asset` forces 1..=1000 for PreIpo assets, so the
   * guard can never be switched off by leaving this at 0 where it applies.
   */
  maxValuationDivergenceBps: number;
  /**
   * Maximum transfer-fee basis points the mint may charge, declared at
   * listing and checked against the mint's own TransferFeeConfig in
   * `add_asset` (the max of the active and scheduled epochs). Deposits
   * credit the vault's *measured* delta, never the requested amount, so
   * a fee can only ever reduce recorded collateral, never inflate debt.
   */
  maxTransferFeeBps: number;
  /** Issuer control flags, recorded from the mint at listing. See CTRL_*. */
  issuerControls: number;
  /** Reserved for future fields; keeps the Borsh layout upgrade-safe. */
  reserved: ReadonlyUint8Array;
};

export type AssetArgs = {
  /** The Token-2022 mint carrying the `ScaledUiAmount` extension. */
  stockMint: Address;
  /**
   * Pyth `Equity.US.<TICKER>/USD` — the real underlying. Only exists in
   * regular session, which is exactly why the haircut widens.
   */
  equityFeedId: ReadonlyUint8Array;
  /** Pyth `Crypto.<TICKER>X/USD` — the token itself. Trades 24/7. */
  tokenFeedId: ReadonlyUint8Array;
  maxLtvRegularBps: number;
  maxLtvExtendedBps: number;
  maxLtvClosedBps: number;
  /** Liquidation triggers at this LTV, using the conservative mark. */
  liqThresholdBps: number;
  /** Never liquidate above this, even outside market hours. Hard floor. */
  liqFloorBps: number;
  /**
   * Discount a liquidator gets on the seized collateral, in bps. This is the
   * incentive to liquidate; without it nobody would bother.
   */
  liquidationBonusBps: number;
  /** Above this confidence ratio the LTV starts decaying. */
  maxConfRatioBps: number;
  /** Confidence floor applied when the equity feed is stale (`Closed`). */
  confFloorBps: number | bigint;
  /**
   * Largest multiplier change accepted as a dividend, in bps. A dividend
   * moves the multiplier by a fraction of a percent; a 2:1 split moves it
   * by 10,000 bps. Anything above this is treated as a corporate action we
   * do not understand and is refused rather than paid out.
   */
  maxMultiplierDeltaBps: number;
  /** Minimum on-chain pool depth, in whole USD, before the asset is usable. */
  minPoolDepthUsd: number | bigint;
  decimals: number;
  enabled: boolean;
  bump: number;
  /**
   * Public equity vs. pre-IPO collateral. Selects the rule set; see
   * `MarketKind`. Immutable after listing (`set_asset_params` will not
   * touch it — flipping the valuation rules under live positions would
   * be repricing someone's collateral behind their back).
   */
  marketKind: MarketKindArgs;
  /**
   * Pre-IPO only: largest absolute divergence in bps between the sized
   * Pyth mark and the issuer's own published mark that `resize_line` will
   * accept. `validate_asset` forces 1..=1000 for PreIpo assets, so the
   * guard can never be switched off by leaving this at 0 where it applies.
   */
  maxValuationDivergenceBps: number;
  /**
   * Maximum transfer-fee basis points the mint may charge, declared at
   * listing and checked against the mint's own TransferFeeConfig in
   * `add_asset` (the max of the active and scheduled epochs). Deposits
   * credit the vault's *measured* delta, never the requested amount, so
   * a fee can only ever reduce recorded collateral, never inflate debt.
   */
  maxTransferFeeBps: number;
  /** Issuer control flags, recorded from the mint at listing. See CTRL_*. */
  issuerControls: number;
  /** Reserved for future fields; keeps the Borsh layout upgrade-safe. */
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link AssetArgs} account data. */
export function getAssetEncoder(): FixedSizeEncoder<AssetArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["stockMint", getAddressEncoder()],
      ["equityFeedId", fixEncoderSize(getBytesEncoder(), 32)],
      ["tokenFeedId", fixEncoderSize(getBytesEncoder(), 32)],
      ["maxLtvRegularBps", getU16Encoder()],
      ["maxLtvExtendedBps", getU16Encoder()],
      ["maxLtvClosedBps", getU16Encoder()],
      ["liqThresholdBps", getU16Encoder()],
      ["liqFloorBps", getU16Encoder()],
      ["liquidationBonusBps", getU16Encoder()],
      ["maxConfRatioBps", getU16Encoder()],
      ["confFloorBps", getU64Encoder()],
      ["maxMultiplierDeltaBps", getU16Encoder()],
      ["minPoolDepthUsd", getU64Encoder()],
      ["decimals", getU8Encoder()],
      ["enabled", getBooleanEncoder()],
      ["bump", getU8Encoder()],
      ["marketKind", getMarketKindEncoder()],
      ["maxValuationDivergenceBps", getU16Encoder()],
      ["maxTransferFeeBps", getU16Encoder()],
      ["issuerControls", getU8Encoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 58)],
    ]),
    (value) => ({ ...value, discriminator: ASSET_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Asset} account data. */
export function getAssetDecoder(): FixedSizeDecoder<Asset> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["stockMint", getAddressDecoder()],
    ["equityFeedId", fixDecoderSize(getBytesDecoder(), 32)],
    ["tokenFeedId", fixDecoderSize(getBytesDecoder(), 32)],
    ["maxLtvRegularBps", getU16Decoder()],
    ["maxLtvExtendedBps", getU16Decoder()],
    ["maxLtvClosedBps", getU16Decoder()],
    ["liqThresholdBps", getU16Decoder()],
    ["liqFloorBps", getU16Decoder()],
    ["liquidationBonusBps", getU16Decoder()],
    ["maxConfRatioBps", getU16Decoder()],
    ["confFloorBps", getU64Decoder()],
    ["maxMultiplierDeltaBps", getU16Decoder()],
    ["minPoolDepthUsd", getU64Decoder()],
    ["decimals", getU8Decoder()],
    ["enabled", getBooleanDecoder()],
    ["bump", getU8Decoder()],
    ["marketKind", getMarketKindDecoder()],
    ["maxValuationDivergenceBps", getU16Decoder()],
    ["maxTransferFeeBps", getU16Decoder()],
    ["issuerControls", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 58)],
  ]);
}

/** Gets the codec for {@link Asset} account data. */
export function getAssetCodec(): FixedSizeCodec<AssetArgs, Asset> {
  return combineCodec(getAssetEncoder(), getAssetDecoder());
}

export function decodeAsset<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Asset, TAddress>;
export function decodeAsset<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Asset, TAddress>;
export function decodeAsset<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Asset, TAddress> | MaybeAccount<Asset, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getAssetDecoder(),
  );
}

export async function fetchAsset<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Asset, TAddress>> {
  const maybeAccount = await fetchMaybeAsset(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeAsset<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Asset, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeAsset(maybeAccount);
}

export async function fetchAllAsset(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Asset>[]> {
  const maybeAccounts = await fetchAllMaybeAsset(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeAsset(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Asset>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeAsset(maybeAccount));
}

export function getAssetSize(): number {
  return 203;
}
