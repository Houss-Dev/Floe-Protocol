/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { LEDGERLINE_PROGRAM_ADDRESS } from "../programs";
import {
  getPriceMarkDecoder,
  getPriceMarkEncoder,
  type PriceMark,
  type PriceMarkArgs,
} from "../types";

export const RESIZE_LINE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  49, 83, 35, 66, 142, 174, 73, 102,
]);

export function getResizeLineDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(RESIZE_LINE_DISCRIMINATOR);
}

export type ResizeLineInstruction<
  TProgram extends string = typeof LEDGERLINE_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountLine extends string | AccountMeta<string> = string,
  TAccountKeeper extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountLine extends string
        ? WritableAccount<TAccountLine>
        : TAccountLine,
      TAccountKeeper extends string
        ? ReadonlySignerAccount<TAccountKeeper> &
            AccountSignerMeta<TAccountKeeper>
        : TAccountKeeper,
      ...TRemainingAccounts,
    ]
  >;

export type ResizeLineInstructionData = {
  discriminator: ReadonlyUint8Array;
  marks: Array<PriceMark>;
};

export type ResizeLineInstructionDataArgs = { marks: Array<PriceMarkArgs> };

export function getResizeLineInstructionDataEncoder(): Encoder<ResizeLineInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["marks", getArrayEncoder(getPriceMarkEncoder())],
    ]),
    (value) => ({ ...value, discriminator: RESIZE_LINE_DISCRIMINATOR }),
  );
}

export function getResizeLineInstructionDataDecoder(): Decoder<ResizeLineInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["marks", getArrayDecoder(getPriceMarkDecoder())],
  ]);
}

export function getResizeLineInstructionDataCodec(): Codec<
  ResizeLineInstructionDataArgs,
  ResizeLineInstructionData
> {
  return combineCodec(
    getResizeLineInstructionDataEncoder(),
    getResizeLineInstructionDataDecoder(),
  );
}

export type ResizeLineAsyncInput<
  TAccountConfig extends InstructionAccountInput = InstructionAccountInput,
  TAccountLine extends InstructionAccountInput = InstructionAccountInput,
  TAccountKeeper extends InstructionSignerInput = InstructionSignerInput,
> = {
  config?: TAccountConfig;
  line: TAccountLine;
  keeper: TAccountKeeper;
  marks: ResizeLineInstructionDataArgs["marks"];
};

export async function getResizeLineInstructionAsync<
  TAccountConfig extends InstructionAccountInput,
  TAccountLine extends InstructionAccountInput,
  TAccountKeeper extends InstructionSignerInput,
  TProgramAddress extends Address = typeof LEDGERLINE_PROGRAM_ADDRESS,
>(
  input: ResizeLineAsyncInput<TAccountConfig, TAccountLine, TAccountKeeper>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ResizeLineInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountConfig,
      InstructionAccountInputAddress<TAccountConfig>
    >,
    ResolvedInstructionAccountMeta<
      TAccountLine,
      InstructionAccountInputAddress<TAccountLine>
    >,
    ResolvedInstructionAccountMeta<
      TAccountKeeper,
      InstructionAccountInputAddress<TAccountKeeper>
    >
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEDGERLINE_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isSigner: false, isWritable: false },
    line: { value: input.line ?? null, isSigner: false, isWritable: true },
    keeper: { value: input.keeper ?? null, isSigner: true, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({ programAddress });
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("line", accounts.line),
      getAccountMeta("keeper", accounts.keeper),
    ],
    data: getResizeLineInstructionDataEncoder().encode(
      args as ResizeLineInstructionDataArgs,
    ),
    programAddress,
  } as ResizeLineInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountConfig,
      InstructionAccountInputAddress<TAccountConfig>
    >,
    ResolvedInstructionAccountMeta<
      TAccountLine,
      InstructionAccountInputAddress<TAccountLine>
    >,
    ResolvedInstructionAccountMeta<
      TAccountKeeper,
      InstructionAccountInputAddress<TAccountKeeper>
    >
  >);
}

export type ResizeLineInput<
  TAccountConfig extends InstructionAccountInput = InstructionAccountInput,
  TAccountLine extends InstructionAccountInput = InstructionAccountInput,
  TAccountKeeper extends InstructionSignerInput = InstructionSignerInput,
> = {
  config: TAccountConfig;
  line: TAccountLine;
  keeper: TAccountKeeper;
  marks: ResizeLineInstructionDataArgs["marks"];
};

export function getResizeLineInstruction<
  TAccountConfig extends InstructionAccountInput,
  TAccountLine extends InstructionAccountInput,
  TAccountKeeper extends InstructionSignerInput,
  TProgramAddress extends Address = typeof LEDGERLINE_PROGRAM_ADDRESS,
>(
  input: ResizeLineInput<TAccountConfig, TAccountLine, TAccountKeeper>,
  config?: { programAddress?: TProgramAddress },
): ResizeLineInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountConfig,
    InstructionAccountInputAddress<TAccountConfig>
  >,
  ResolvedInstructionAccountMeta<
    TAccountLine,
    InstructionAccountInputAddress<TAccountLine>
  >,
  ResolvedInstructionAccountMeta<
    TAccountKeeper,
    InstructionAccountInputAddress<TAccountKeeper>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEDGERLINE_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isSigner: false, isWritable: false },
    line: { value: input.line ?? null, isSigner: false, isWritable: true },
    keeper: { value: input.keeper ?? null, isSigner: true, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("line", accounts.line),
      getAccountMeta("keeper", accounts.keeper),
    ],
    data: getResizeLineInstructionDataEncoder().encode(
      args as ResizeLineInstructionDataArgs,
    ),
    programAddress,
  } as ResizeLineInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountConfig,
      InstructionAccountInputAddress<TAccountConfig>
    >,
    ResolvedInstructionAccountMeta<
      TAccountLine,
      InstructionAccountInputAddress<TAccountLine>
    >,
    ResolvedInstructionAccountMeta<
      TAccountKeeper,
      InstructionAccountInputAddress<TAccountKeeper>
    >
  >);
}

export type ParsedResizeLineInstruction<
  TProgram extends string = typeof LEDGERLINE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    line: TAccountMetas[1];
    keeper: TAccountMetas[2];
  };
  data: ResizeLineInstructionData;
};

export function parseResizeLineInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedResizeLineInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      line: getNextAccount(),
      keeper: getNextAccount(),
    },
    data: getResizeLineInstructionDataDecoder().decode(instruction.data),
  };
}
