/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const DEPOSIT_EVENT_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([120, 248, 61, 83, 31, 142, 107, 144]);

export function getDepositEventEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DEPOSIT_EVENT_EVENT_DISCRIMINATOR,
  );
}

export type DepositEventEvent = {
  line: Address;
  mint: Address;
  /**
   * Raw token units actually received by the vault (after any transfer
   * fee), never the amount the sender requested.
   */
  amount: bigint;
  seq: bigint;
};

export type DepositEventEventArgs = {
  line: Address;
  mint: Address;
  /**
   * Raw token units actually received by the vault (after any transfer
   * fee), never the amount the sender requested.
   */
  amount: number | bigint;
  seq: number | bigint;
};

/** Gets the encoder for {@link DepositEventEventArgs} event data. */
export function getDepositEventEventEncoder(): FixedSizeEncoder<DepositEventEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["line", getAddressEncoder()],
      ["mint", getAddressEncoder()],
      ["amount", getU64Encoder()],
      ["seq", getU64Encoder()],
    ]),
    [getConstantEncoder(DEPOSIT_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link DepositEventEvent} event data. */
export function getDepositEventEventDecoder(): FixedSizeDecoder<DepositEventEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["line", getAddressDecoder()],
      ["mint", getAddressDecoder()],
      ["amount", getU64Decoder()],
      ["seq", getU64Decoder()],
    ]),
    [getConstantDecoder(DEPOSIT_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link DepositEventEvent} event data. */
export function getDepositEventEventCodec(): FixedSizeCodec<
  DepositEventEventArgs,
  DepositEventEvent
> {
  return combineCodec(
    getDepositEventEventEncoder(),
    getDepositEventEventDecoder(),
  );
}

/** Parses the provided bytes into {@link DepositEventEvent} event data. */
export function parseDepositEventEvent(
  data: ReadonlyUint8Array | Uint8Array,
): DepositEventEvent {
  if (containsBytes(data, DEPOSIT_EVENT_EVENT_DISCRIMINATOR, 0)) {
    return getDepositEventEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "DepositEventEvent" event discriminators.',
  );
}
