/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const DRAW_EVENT_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([232, 110, 40, 177, 104, 185, 49, 59]);

export function getDrawEventEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DRAW_EVENT_EVENT_DISCRIMINATOR,
  );
}

export type DrawEventEvent = {
  line: Address;
  /** USDC disbursed to the borrower (6dp minor units). */
  amount: bigint;
  /** Principal + draw fee added to debt. */
  totalDebtIncrease: bigint;
  seq: bigint;
};

export type DrawEventEventArgs = {
  line: Address;
  /** USDC disbursed to the borrower (6dp minor units). */
  amount: number | bigint;
  /** Principal + draw fee added to debt. */
  totalDebtIncrease: number | bigint;
  seq: number | bigint;
};

/** Gets the encoder for {@link DrawEventEventArgs} event data. */
export function getDrawEventEventEncoder(): FixedSizeEncoder<DrawEventEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["line", getAddressEncoder()],
      ["amount", getU64Encoder()],
      ["totalDebtIncrease", getU64Encoder()],
      ["seq", getU64Encoder()],
    ]),
    [getConstantEncoder(DRAW_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link DrawEventEvent} event data. */
export function getDrawEventEventDecoder(): FixedSizeDecoder<DrawEventEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["line", getAddressDecoder()],
      ["amount", getU64Decoder()],
      ["totalDebtIncrease", getU64Decoder()],
      ["seq", getU64Decoder()],
    ]),
    [getConstantDecoder(DRAW_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link DrawEventEvent} event data. */
export function getDrawEventEventCodec(): FixedSizeCodec<
  DrawEventEventArgs,
  DrawEventEvent
> {
  return combineCodec(getDrawEventEventEncoder(), getDrawEventEventDecoder());
}

/** Parses the provided bytes into {@link DrawEventEvent} event data. */
export function parseDrawEventEvent(
  data: ReadonlyUint8Array | Uint8Array,
): DrawEventEvent {
  if (containsBytes(data, DRAW_EVENT_EVENT_DISCRIMINATOR, 0)) {
    return getDrawEventEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "DrawEventEvent" event discriminators.',
  );
}
