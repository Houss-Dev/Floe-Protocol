/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const DIVIDEND_HARVESTED_EVENT_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([110, 255, 185, 86, 185, 186, 15, 142]);

export function getDividendHarvestedEventEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DIVIDEND_HARVESTED_EVENT_EVENT_DISCRIMINATOR,
  );
}

export type DividendHarvestedEventEvent = {
  line: Address;
  mint: Address;
  effectiveTs: bigint;
  /** USDC of dividend value converted and applied (6dp). */
  netUsd: bigint;
  /** Collateral trimmed to keep share-count parity after the multiplier bump. */
  rawTrimmed: bigint;
  seq: bigint;
};

export type DividendHarvestedEventEventArgs = {
  line: Address;
  mint: Address;
  effectiveTs: number | bigint;
  /** USDC of dividend value converted and applied (6dp). */
  netUsd: number | bigint;
  /** Collateral trimmed to keep share-count parity after the multiplier bump. */
  rawTrimmed: number | bigint;
  seq: number | bigint;
};

/** Gets the encoder for {@link DividendHarvestedEventEventArgs} event data. */
export function getDividendHarvestedEventEventEncoder(): FixedSizeEncoder<DividendHarvestedEventEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["line", getAddressEncoder()],
      ["mint", getAddressEncoder()],
      ["effectiveTs", getI64Encoder()],
      ["netUsd", getU64Encoder()],
      ["rawTrimmed", getU64Encoder()],
      ["seq", getU64Encoder()],
    ]),
    [getConstantEncoder(DIVIDEND_HARVESTED_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link DividendHarvestedEventEvent} event data. */
export function getDividendHarvestedEventEventDecoder(): FixedSizeDecoder<DividendHarvestedEventEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["line", getAddressDecoder()],
      ["mint", getAddressDecoder()],
      ["effectiveTs", getI64Decoder()],
      ["netUsd", getU64Decoder()],
      ["rawTrimmed", getU64Decoder()],
      ["seq", getU64Decoder()],
    ]),
    [getConstantDecoder(DIVIDEND_HARVESTED_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link DividendHarvestedEventEvent} event data. */
export function getDividendHarvestedEventEventCodec(): FixedSizeCodec<
  DividendHarvestedEventEventArgs,
  DividendHarvestedEventEvent
> {
  return combineCodec(
    getDividendHarvestedEventEventEncoder(),
    getDividendHarvestedEventEventDecoder(),
  );
}

/** Parses the provided bytes into {@link DividendHarvestedEventEvent} event data. */
export function parseDividendHarvestedEventEvent(
  data: ReadonlyUint8Array | Uint8Array,
): DividendHarvestedEventEvent {
  if (containsBytes(data, DIVIDEND_HARVESTED_EVENT_EVENT_DISCRIMINATOR, 0)) {
    return getDividendHarvestedEventEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "DividendHarvestedEventEvent" event discriminators.',
  );
}
