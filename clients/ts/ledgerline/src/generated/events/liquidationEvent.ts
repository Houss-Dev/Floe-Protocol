/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const LIQUIDATION_EVENT_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([3, 13, 21, 93, 173, 136, 72, 144]);

export function getLiquidationEventEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LIQUIDATION_EVENT_EVENT_DISCRIMINATOR,
  );
}

export type LiquidationEventEvent = {
  line: Address;
  /** USDC of debt the liquidator repaid. */
  repaid: bigint;
  /** Collateral raw units seized (value includes the bonus). */
  seized: bigint;
  seq: bigint;
};

export type LiquidationEventEventArgs = {
  line: Address;
  /** USDC of debt the liquidator repaid. */
  repaid: number | bigint;
  /** Collateral raw units seized (value includes the bonus). */
  seized: number | bigint;
  seq: number | bigint;
};

/** Gets the encoder for {@link LiquidationEventEventArgs} event data. */
export function getLiquidationEventEventEncoder(): FixedSizeEncoder<LiquidationEventEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["line", getAddressEncoder()],
      ["repaid", getU64Encoder()],
      ["seized", getU64Encoder()],
      ["seq", getU64Encoder()],
    ]),
    [getConstantEncoder(LIQUIDATION_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link LiquidationEventEvent} event data. */
export function getLiquidationEventEventDecoder(): FixedSizeDecoder<LiquidationEventEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["line", getAddressDecoder()],
      ["repaid", getU64Decoder()],
      ["seized", getU64Decoder()],
      ["seq", getU64Decoder()],
    ]),
    [getConstantDecoder(LIQUIDATION_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link LiquidationEventEvent} event data. */
export function getLiquidationEventEventCodec(): FixedSizeCodec<
  LiquidationEventEventArgs,
  LiquidationEventEvent
> {
  return combineCodec(
    getLiquidationEventEventEncoder(),
    getLiquidationEventEventDecoder(),
  );
}

/** Parses the provided bytes into {@link LiquidationEventEvent} event data. */
export function parseLiquidationEventEvent(
  data: ReadonlyUint8Array | Uint8Array,
): LiquidationEventEvent {
  if (containsBytes(data, LIQUIDATION_EVENT_EVENT_DISCRIMINATOR, 0)) {
    return getLiquidationEventEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "LiquidationEventEvent" event discriminators.',
  );
}
