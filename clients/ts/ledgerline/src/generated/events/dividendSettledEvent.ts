/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const DIVIDEND_SETTLED_EVENT_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([49, 248, 123, 147, 204, 85, 138, 39]);

export function getDividendSettledEventEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DIVIDEND_SETTLED_EVENT_EVENT_DISCRIMINATOR,
  );
}

export type DividendSettledEventEvent = {
  line: Address;
  appliedToDebt: bigint;
  paidToUser: bigint;
  seq: bigint;
};

export type DividendSettledEventEventArgs = {
  line: Address;
  appliedToDebt: number | bigint;
  paidToUser: number | bigint;
  seq: number | bigint;
};

/** Gets the encoder for {@link DividendSettledEventEventArgs} event data. */
export function getDividendSettledEventEventEncoder(): FixedSizeEncoder<DividendSettledEventEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["line", getAddressEncoder()],
      ["appliedToDebt", getU64Encoder()],
      ["paidToUser", getU64Encoder()],
      ["seq", getU64Encoder()],
    ]),
    [getConstantEncoder(DIVIDEND_SETTLED_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link DividendSettledEventEvent} event data. */
export function getDividendSettledEventEventDecoder(): FixedSizeDecoder<DividendSettledEventEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["line", getAddressDecoder()],
      ["appliedToDebt", getU64Decoder()],
      ["paidToUser", getU64Decoder()],
      ["seq", getU64Decoder()],
    ]),
    [getConstantDecoder(DIVIDEND_SETTLED_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link DividendSettledEventEvent} event data. */
export function getDividendSettledEventEventCodec(): FixedSizeCodec<
  DividendSettledEventEventArgs,
  DividendSettledEventEvent
> {
  return combineCodec(
    getDividendSettledEventEventEncoder(),
    getDividendSettledEventEventDecoder(),
  );
}

/** Parses the provided bytes into {@link DividendSettledEventEvent} event data. */
export function parseDividendSettledEventEvent(
  data: ReadonlyUint8Array | Uint8Array,
): DividendSettledEventEvent {
  if (containsBytes(data, DIVIDEND_SETTLED_EVENT_EVENT_DISCRIMINATOR, 0)) {
    return getDividendSettledEventEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "DividendSettledEventEvent" event discriminators.',
  );
}
